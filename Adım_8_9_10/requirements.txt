# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Dependency Injection (Bonus)
dependency-injector>=4.41.0

# RAG — Embedding
sentence-transformers>=2.7.0

# RAG — Vector Store
faiss-cpu>=1.8.0

# Data
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0  # parquet okuma

# HTTP (LM Studio client)
requests>=2.31.0

# ML (Adım 4 anomali detection için)
scikit-learn>=1.4.0

# YAML (Rule Engine kural dosyası)
pyyaml>=6.0.0

# Utilities
python-dotenv>=1.0.0
